@model AgendadorVacina.Models.Local

@{
    ViewData["Title"] = "Details";
}

<h2>Quantidades Vagas do Posto</h2>

<ul>
    @for (int i = 0; i < (int)ViewData["NumTimes"]; i++)
    {
        <li>@ViewData["Message"]</li>
    }
</ul>